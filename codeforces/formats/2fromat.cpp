#include <bits/stdc++.h>

#define REP(i, a, b) for(int i =a; i<b; i++)  // macro to shorten the for loop

using namespace std;
typedef long long ll;
typedef vector<int> vi;
// now we can use long long as ll and vector<int> as vi

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin>>t;
    REP(i,0,t){
        cout<< i<< '\n';
    }
    return 0;
}